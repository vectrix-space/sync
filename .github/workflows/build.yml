name: "build"

on:
  push:
    branches: "**"
    tags-ignore: ["**"]
  pull_request:
  release:
    types: [released]

jobs:
  call-build:
    uses: "vectrix-space/.github/.github/workflows/shared-ci.yaml@main"
    secrets: "inherit"
